<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/8bb78a721a.js" crossorigin="anonymous"></script>
</head>
<body>
    <h1>Listado de tus resenas</h1>
    <div class="container p-4">
<div class="row">
     <% for(let i = 0; i < todo.length; i++) { %>
            <div class="col-md-3">
                <div class="card text-center">
                <div class="card-body">
                    <h3 class="card-title text-uppercase"><%= todo[i].title%></h3> 
            <p class="m-2"> <%= todo[i].description%></p>
            <p class="m-2"> <%= todo[i].createdAt%></p>
            <form action="/login/delete/<%= todo[i].resena_id %>" method="POST">
                <button class="btn btn-danger">Delete Resena</button>
            </form>
            <form action="/login/edit/<%= todo[i].resena_id %>" method="GET">
                <button class="btn btn-secondary">Modify</button>
            </form>
            
        </div>  
    </div>  
                </div>
    <% } %>
</div>
</div>
    <div class="col-md-4 mx-auto">
        <div class="card car-body text-center">
            <form action="/login/add" method="GET">
                <button class="btn btn-success btn-block"> Crea uno!</button>
            </form>
        </div>
    </div>
</body>
</html>